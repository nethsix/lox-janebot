# Slack App Tokens
# Create a Slack app at https://api.slack.com/apps
# Enable Socket Mode and get an App-Level Token (xapp-...)
# Install the app to your workspace and get a Bot Token (xoxb-...)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SIGNING_SECRET=your-signing-secret

# Amp SDK
# Get your access token at https://ampcode.com/settings
AMP_ACCESS_TOKEN=your-amp-access-token

# Configuration
WORKSPACE_DIR=/path/to/your/workspace
AGENT_MODE=smart

# Debouncing (ms to wait for additional messages)
DEBOUNCE_MS=1500

# Maximum response length before truncating
MAX_RESPONSE_LENGTH=10000

# Authorization (comma-separated, empty = allow all)
ALLOWED_USER_IDS=
ALLOWED_CHANNEL_IDS=

# MCP Servers (format: "name:command:arg1,arg2;name2:command2:arg1")
# Example: "github:npx:-y,@anthropic/mcp-server-github"
MCP_SERVERS=
