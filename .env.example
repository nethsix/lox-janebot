# Slack App Tokens
# Create a Slack app at https://api.slack.com/apps
# Enable Socket Mode and get an App-Level Token (xapp-...)
# Install the app to your workspace and get a Bot Token (xoxb-...)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SIGNING_SECRET=your-signing-secret

# Amp SDK
# Get your access token at https://ampcode.com/settings
AMP_API_KEY=your-amp-api-key

# GitHub App (optional - enables GitHub access in sprites via short-lived tokens)
# Create an app at https://github.com/organizations/buildkite/settings/apps
# Permissions: Contents (read & write), Pull requests (read & write), Metadata (read)
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_APP_INSTALLATION_ID=

# Configuration
WORKSPACE_DIR=/path/to/your/workspace
AGENT_MODE=smart

# Debouncing (ms to wait for additional messages)
DEBOUNCE_MS=1500

# Maximum response length before truncating
MAX_RESPONSE_LENGTH=10000

# Authorization (comma-separated, empty = allow all)
ALLOWED_USER_IDS=
ALLOWED_CHANNEL_IDS=

# MCP Servers (format: "name:command:arg1,arg2;name2:command2:arg1")
# Example: "github:npx:-y,@anthropic/mcp-server-github"
MCP_SERVERS=

# Git identity for commits made in sprites (optional)
GIT_AUTHOR_NAME=
GIT_AUTHOR_EMAIL=

# Execution environment (one of these is required)
#
# Option 1: Sprites sandbox (recommended for multi-user)
# Each Slack thread gets an isolated VM
# Get your token at https://sprites.dev/account
SPRITES_TOKEN=
#
# Option 2: Local execution (only for trusted single-user setups)
# Runs amp directly on the host machine - NO SANDBOX
# ALLOW_LOCAL_EXECUTION=true
